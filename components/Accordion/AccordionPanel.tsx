import type { AccordionPanelProps } from './types';

/**
 * AccordionPanel - Declarative panel for Accordion composition
 * Rendering is handled by the parent Accordion component
 */
const AccordionPanel = (_props: AccordionPanelProps) => null;

export default AccordionPanel;
